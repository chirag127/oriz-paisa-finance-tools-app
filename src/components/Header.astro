---
import HeaderControls from '~/components/HeaderControls.tsx'
import { SITE_CONFIG, SITE_EMOJI } from '~/lib/siteConfig'
import { FAMILY } from '@chirag127/oriz-ui'
---

<header class="site-header">
  <div class="container-page header-inner">
    <a href={FAMILY.rootOrigin} class="brand" aria-label="oriz hub">
      <span class="brand-mark">oriz</span>
      <span class="brand-dot">.</span>
    </a>

    <a href="/" class="site-badge" aria-label={SITE_CONFIG.name}>
      <span class="site-emoji" aria-hidden="true">{SITE_EMOJI}</span>
      <span class="site-name">{SITE_CONFIG.name}</span>
    </a>

    <nav aria-label="Primary" class="primary-nav">
      <a href="/">Home</a>
      <a href="/about/">About</a>
      <a href="/contact/">Contact</a>
    </nav>

    <HeaderControls client:idle siteName={SITE_CONFIG.name} />
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in oklab, var(--color-bg) 85%, transparent);
    backdrop-filter: saturate(180%) blur(20px);
    -webkit-backdrop-filter: saturate(180%) blur(20px);
    border-bottom: 1px solid var(--color-border);
  }
  .header-inner {
    height: 64px;
    display: flex;
    align-items: center;
    gap: 1rem;
  }
  .brand {
    font-family: var(--font-serif);
    font-size: 1.25rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    text-decoration: none;
    color: var(--color-fg);
    line-height: 1;
  }
  .brand-dot { color: var(--color-accent); }
  .site-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.25rem 0.625rem;
    background: var(--color-bg-soft);
    border: 1px solid var(--color-border);
    border-radius: 999px;
    color: var(--color-fg);
    font-size: 0.875rem;
    text-decoration: none;
    line-height: 1;
  }
  .site-badge:hover {
    border-color: color-mix(in oklab, var(--color-accent) 50%, var(--color-border));
    color: var(--color-fg);
  }
  .site-emoji { font-size: 1rem; }
  .primary-nav {
    display: none;
    gap: 1.5rem;
    flex: 1;
    font-size: 0.9375rem;
  }
  .primary-nav a {
    color: var(--color-fg-muted);
    text-decoration: none;
  }
  .primary-nav a:hover { color: var(--color-fg); }
  @media (min-width: 768px) {
    .primary-nav { display: flex; }
  }
</style>
